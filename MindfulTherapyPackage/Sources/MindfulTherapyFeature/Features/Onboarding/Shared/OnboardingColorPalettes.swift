import SwiftUI

// MARK: - Shared Color Palettes for Onboarding Screens

// MARK: - Perfect Blend Color Palette
public struct PerfectBlendColors {
    public let primary = Color(hex: "1a1a1a")        // Near black
    public let secondary = Color(hex: "4a4a4a")       // Dark gray
    public let accent = Color(hex: "6a6a6a")          // Medium gray
    public let tertiary = Color(hex: "8a8a8a")        // Light gray
    
    public let background = Color(hex: "fefefe")       // Pure white
    public let surfacePrimary = Color.white            // White cards
    public let surfaceSecondary = Color(hex: "f8f8f8") // Light gray backgrounds
    
    public let textPrimary = Color(hex: "1a1a1a")     // Near black text
    public let textSecondary = Color(hex: "6a6a6a")    // Medium gray text
    public let textTertiary = Color(hex: "9a9a9a")     // Light gray text
    
    public let border = Color(hex: "e8e8e8")           // Very light gray borders
    
    public init() {}
}

// MARK: - Soft & Supportive Color Palette
public struct SoftSupportiveColors {
    public let primary = Color(hex: "8B7355")          // Warm taupe
    public let secondary = Color(hex: "9A8B7A")        // Soft brown
    public let accent = Color(hex: "D4B5A0")           // Soft beige
    public let tertiary = Color(hex: "C4B5A0")         // Warm beige
    
    public let background = Color(hex: "FEFEFE")       // Pure white
    public let surfacePrimary = Color.white            // White cards
    public let surfaceSecondary = Color(hex: "FAF8F6") // Off-white backgrounds
    
    public let textPrimary = Color(hex: "2A2A2A")      // Soft black text
    public let textSecondary = Color(hex: "6B6B6B")    // Medium gray text
    public let textTertiary = Color(hex: "9B9B9B")     // Light gray text
    
    public let border = Color(hex: "F0E6DC")           // Very light beige borders
    
    public init() {}
}

// MARK: - Clean & Focused Color Palette
public struct CleanFocusedColors {
    public let primary = Color(hex: "1a1a1a")          // Near black
    public let secondary = Color(hex: "4a4a4a")         // Dark gray
    public let accent = Color(hex: "6a6a6a")            // Medium gray
    public let tertiary = Color(hex: "8a8a8a")          // Light gray
    
    public let background = Color(hex: "fefefe")        // Pure white
    public let surfacePrimary = Color.white             // White cards
    public let surfaceSecondary = Color(hex: "f8f8f8")  // Light gray backgrounds
    
    public let textPrimary = Color(hex: "1a1a1a")      // Near black text
    public let textSecondary = Color(hex: "6a6a6a")     // Medium gray text
    public let textTertiary = Color(hex: "9a9a9a")      // Light gray text
    
    public let border = Color(hex: "e8e8e8")            // Very light gray borders
    
    public init() {}
}